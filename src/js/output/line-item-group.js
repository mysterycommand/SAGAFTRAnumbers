// Generated by CoffeeScript 1.6.1
(function() {
  var define, log, root;

  root = this;

  log = root.log;

  define = root.define;

  define(['jquery'], function($) {
    var LineItemGroup;
    LineItemGroup = (function() {

      function LineItemGroup(className) {
        this.classNames = ['line-item-group'];
        this.classNames.push(className);
        return;
      }

      LineItemGroup.prototype.estimate = function(data) {
        var item, _i, _len, _ref;
        this.data = data;
        this.html = "";
        this.cost = 0;
        this.html += "<tbody class=\"" + (this.classNames.join(' ')) + "\">\n  <tr>\n    <th colspan=\"2\" headers=\"line-item-label\">" + this.data.label + "</th>\n  </tr>";
        if (this.data.first != null) {
          this.html += "<tr>\n  <td headers=\"line-item-label\" class=\"label\">" + this.data.first.label + "</td>\n  <td headers=\"line-item-amount\" class=\"amount\">$ " + (this.data.first.price.toFixed(2)) + "</td>\n</tr>";
          this.cost += this.data.first.price;
        }
        _ref = this.data.items;
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          item = _ref[_i];
          this.html += "<tr>\n  <td headers=\"line-item-label\" class=\"label\">" + (item.count > 0 ? item.count.toString() + ' ' : '') + item.label + "</td>\n  <td headers=\"line-item-amount\" class=\"amount\">$ " + (item.count > 0 ? (item.count * item.price).toFixed(2) : item.price.toFixed(2)) + "</td>\n</tr>";
          this.cost += item.count > 0 ? item.count * item.price : item.price;
        }
        this.html += "</tbody>";
      };

      return LineItemGroup;

    })();
    return LineItemGroup;
  });

}).call(this);
